const os = require('os');

/* for getting the OS arch like my os is 32bit or 64 bit */
/* for_os */

//console.log(os.arch());

/* for getting the available RAM   */

/* const availableRamSize = os.freemem();
console.log(`currently free RAM memory size in  ${availableRamSize} Bites`);
console.log(`currently free RAM memory size in  ${availableRamSize/1024/1024} MBs`);
console.log(`currently free RAM memory size in  ${availableRamSize /1024/1024/1024} GBs`); */

/* for getting the Total size of RAM */
//const totalRamSize = os.totalmem();
//console.log(`total RAM size - ${totalRamSize/1024/1024/1024}`);

/* for getting the hostName */
//console.log(`My Host name is -> ${os.hostname()}`)

/* there are many more commands available ...visit the node.js website */
